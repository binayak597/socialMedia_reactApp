import { Box } from "@mui/material";
import { styled } from "@mui/system";

const SpaceBetween = styled(Box)({
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center"
})

export default SpaceBetween //style component 